import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function AdminFieldLayout() {
  return (
    <Card className="rounded-2xl">
      <CardHeader>
        <CardTitle>Admin Field Layout</CardTitle>
      </CardHeader>
      <CardContent className="text-sm text-slate-600">
        Stand status editing canvas placeholder.
      </CardContent>
    </Card>
  );
}
